[package]
name = "ggml-sys"
version = "0.1.1"
repository = { workspace = true }
edition = "2021"
description = "Raw bindings (i.e. bindgen output) for the ggml library."
license = "MIT"

[build-dependencies]
cc = "^1.0"

[features]
cublas = []
clblast = []
