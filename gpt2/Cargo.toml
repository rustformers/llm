[package]
name = "gpt2"
version = { workspace = true }
edition = "2021"

[dependencies]
ggml-rs = { path = "../ggml-rs" }
llm-base = { path = "../llm-base" }

bytemuck = { workspace = true }

[dev-dependencies]
rand = { workspace = true }
